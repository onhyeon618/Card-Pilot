<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <!-- KB국민카드 -->
        <package android:name="com.kbcard.cxh.appcard" />
        <!-- 현대카드 -->
        <package android:name="com.hyundaicard.appcard" />
        <!-- 신한카드 -->
        <package android:name="com.shcard.smartpay" />
        <!-- 롯데카드 -->
        <package android:name="com.lcacApp" />
        <!-- 하나카드 -->
        <package android:name="com.hanaskcard.paycla" />
        <!-- 우리카드 -->
        <package android:name="com.wooricard.smartapp" />
        <!-- NH농협카드 -->
        <package android:name="nh.smart.nhallonepay" />
        <!-- 네이버페이 -->
        <package android:name="com.naverfin.payapp" />
    </queries>

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".CardPilotApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CardPilot">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CardPilot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.CardPilotNotificationListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>